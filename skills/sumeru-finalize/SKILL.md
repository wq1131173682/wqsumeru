---
name: sumeru-finalize
description: 小说完稿校验与导出，也负责小说项�?build/release。用户说小说写完了、要检查错别字/标点/语法、检测敏感词、整理发布版、排版、构建发布包、导出起�?番茄/晋江/纵横等平台格式、多平台导出、自动拆章、标题优化、简介适配时必须使用本技能�?
version: 1.2.0
type: skill
user-invocable: true
---

> 依赖 `sumeru-rules`，默�?`quiet` 模式�?

## 网文完稿校验

### 触发关键�?
小说写完了帮我检查下、导出适合起点/番茄的格式、查有没有错别字、检测敏感词、整理成发布版本、帮我导出小说发布格式、检查小说错别字、敏感词检测、小说完稿检查、适配起点格式导出、番茄小说格式导出、小说排版整理、多平台格式导出、小说完稿导�?

### 核心功能
1. 错别字、标点符号、语法错误检�?
2. 敏感内容、违规内容排查（三级分类�?
3. 格式规范统一：章节标题、段落格式、标点规�?
4. 全文字数统计、完稿报告生�?
5. 适配不同平台发布格式导出
6. 批量替换功能
7. 自动分段功能

### Skill 边界
- 技术性文字校验（错别�?标点/语法）由�?Skill 负责
- `sumeru-polish` 专注文笔和内容层面优化，两者互补不重叠
- 不改变剧情事实、人物关系、伏笔状态和章节结尾钩子
- 只在完稿、发布前检查或用户明确要求导出时触发；普通写作、审查、润色阶段不提前运行

### 独立调用自举
1. 定位项目根目录，读取或生�?`.sumeru/project.json`、`.sumeru/status.json`
2. 根据 `chapters/` 推断可导出章�?
3. 若缺�?tests �?issue summary，不阻塞技术校�?
4. 若缺少当前范围的 context pack，先生成临时 context pack

### 按模式导�?
| 模式 | 输出内容 |
|------|----------|
| `short/light` | `publish/release.md` 或单篇发布稿，基础检�?|
| `medium/standard` | 导出全文和分章版本，生成 `publish/` �?release 检查报�?|
| `long/full` | 完整 build/release，生�?`publish/`、`tests/release-check-report.md`、`build-manifest.json` |

### Build 前检�?
- 读取 `.sumeru/status.json`，默认只导出状态为 `finalized` 的章�?
- 检�?`chapters/` 是否缺章、重章、命名不规范
- 检查正文是否包�?`TODO`、`FIXME`、未替换占位�?
- 检�?`.sumeru/issues.md` 是否存在未关闭的 `critical` �?`major` issue；旧�?`.sumeru/issues/index.json` 只读兼容
- 导出�?`publish/` 时必须剥离章节首行的 `SUMERU_STATUS` 注释
- 若存�?`.sumeru/intro.md`，将简介写入各平台导出版本的开头（起点/番茄/纵横等）
- **平台内容适配检�?*（新增）：导出前执行全平台通用叙事效率检查，对标七猫/番茄免费阅读标准

#### 平台内容适配检�?

> **定位**：Build 前的最终质量门禁。检查结果不阻塞导出（不修改已有章节），但输出适配评分和警告供作者决策�?
>
> **核心指标阈值定�?*�?`sumeru-rules/SKILL.md` 第十三部�?平台适配规则索引"。本节仅定义 build 阶段的检查流程和输出格式�?
>
> **�?review 的分�?*：`sumeru-review` 负责审查阶段的平台适配检查（可修改章节）；`sumeru-finalize` 负责 build 前的最终门禁（仅报告，不修改）�?

##### 检查项�?

| 检查项 | 阈�?| 严重�?| 说明 |
|--------|------|--------|------|
| 开篇前300字冲突启�?| �?00�?| critical | 超限标记 `build_hook_delay` |
| 开篇前300字设定铺�?| 0�?| high | 存在标记 `build_hook_expo` |
| �?-3章结尾钩�?| 每章必须�?| critical | 缺失标记 `build_hook_missing` |
| 全局对话占比 | �?5% | medium | 每章独立计算，汇总报�?|
| 全局内心独白占比 | �?0% | medium | 每章独立计算，汇总报�?|
| 连续低密度警�?| �?�?| medium | 连续3章以上低密度标记 `build_density_low` |
| 章节字数范围 | �?`project.json` �?`chapterWordRange` | low | 标注超出范围的章�?|

##### 检查流�?

1. 父Agent遍历全部章节，逐章计算叙事效率指标（对话占�?独白占比/描写占比�?
2. �?章额外执行钩子强度检�?
3. 汇总各章指�?�?生成 `build-quality-report.md`
4. 问题按严重度分级输出警告，不修改章节内容

##### 输出格式

```markdown
## build-quality-report.md（写�?`.sumeru/finalize/`�?

### 综合评分
| 维度 | 评分 | 说明 |
|------|------|------|
| 开篇钩�?| ⚠️ 2/4 | �?章前198字工作铺垫，未达�?|
| 叙事效率 | ⚠️ 18/30�?| 12章对话占比不�?5% |
| 信息密度 | ⚠️ �?�?| 连续7章低密度，每章仅1个核心事�?|

### 平台适配建议
- **七猫/番茄**：不推荐直接投递。开篇钩子不�?+ �?章节奏过�?
- **晋江**：可投递，建议优化对话占比后发�?
- **起点**：可投递，章节字数偏短但精品文可接�?
```

##### �?review 的分�?

| 阶段 | 负责内容 | 修改权限 |
|------|---------|---------|
| **sumeru-review** | 叙事效率 + 开篇钩子审查，输出 fix-plan | �?可修改章�?|
| **sumeru-finalize** | Build前质量门禁，输出评分+平台适配建议 | �?不修改章节，仅报�?|

### 子Agent并行校验机制

#### 架构设计原则

finalize 采用**父Agent脚本预处�?+ 子Agent待定项判�?*的两阶段架构�?

| 阶段 | 执行�?| 任务 | 原因 |
|------|--------|------|------|
| **第一阶段** | 父Agent | 脚本预处�?| 规则明确、可脚本化、无需AI判断 |
| **第二阶段** | 子Agent | 待定项判�?| 需要上下文理解、语义分�?|

#### 为什么父Agent直接执行脚本�?

1. **确定性任�?*：错别字检查、敏感词初筛、格式规范等任务有明确规则，脚本执行更可�?
2. **性能考虑**：脚本批量处理比子Agent逐条判断更快
3. **一致性保�?*：脚本输出稳定，不受AI随机性影�?
4. **成本优化**：减少子Agent调用次数，降低token消�?

#### 父Agent直接执行的任�?

| 任务 | 脚本 | 说明 |
|------|------|------|
| 错别字检�?| `scripts/spell-check.py` | 词典匹配 + 上下文验�?|
| 敏感词初�?| `scripts/sensitive-word-filter.py` | 正则匹配 + 三级分类 |
| 格式规范 | `scripts/format-validator.py` | 章节标题、段落格式、标点规�?|
| 平台导出 | `scripts/platform-export.py` | 多平台格式转�?|
| Build前检�?| 内置逻辑 | 缺章检查、TODO检查、issue检�?|

#### 子Agent处理的任�?

子Agent只处理脚本标记出�?*待定�?*（需要上下文判断的敏感词）：

| 任务 | 说明 |
|------|------|
| 敏感词上下文判断 | 判断敏感词在上下文中是否安全 |
| 替换建议 | 如果需要替换，提供合适的替代�?|

**处理流程**�?
```
1. 父Agent运行脚本 �?生成待定项列表（最�?0个）
2. 父Agent生成 context pack（含待定�?前后100字符上下文）
3. 子Agent读取 context pack �?判断每个待定�?
4. 子Agent返回处理建议 �?写入 pending-results.json
5. 父Agent根据建议执行最终修�?
```

**待定项返回协议：**
```json
// .sumeru/finalize/pending-results.json
{
  "results": [
    {
      "chapter": "005",
      "word": "杀人灭�?,
      "position": 1234,
      "verdict": "safe|remove|replace",
      "reason": "此处为角色对话中的威胁用语，非实际暴力描�?,
      "replacement": null
    }
  ]
}
```

### 各平台导出格式规�?

#### 导出类型

| 导出类型 | 格式规则 |
|----------|----------|
| **分章导出** | 每章独立文件，标题格式统一�?`第X�?标题xxxx`，章节号用阿拉伯数字不加前导�?|
| **总导�?* | 去掉章节标题，采用书名作为总标题，章节内容连续排版，仅用空行分隔各�?|

> 各平台格式规则和智能导出由脚本处理，详见 `scripts/platform-export.py`�?

### 敏感词检测标�?

| 级别 | 说明 | 处理方式 |
|------|------|----------|
| 一�?| 违反法律法规、政治敏感、色情淫秽、暴力恐怖等 | 必须修改 |
| 二级 | 血腥暴力、低俗用语、医疗描写、未成年人不当内容等 | 建议修改 |
| 三级 | 网络用语过多、易歧义表述、争议话题等 | 优化建议 |

### 数据持久�?
**用户可见输出**�?
- `publish/`：各平台导出版本（含简介）
- `tests/release-check-report.md`：发布前检查报�?

**中间数据（`.sumeru/finalize/`�?*�?
- `clean/full-text.md`、`clean/chapters/`、`error-report.json`、`stats.json`、`build-manifest.json`

**项目元数�?*�?
- `.sumeru/intro.md`：小说简介（大纲完成后自动生成）

### 与其�?Skill 配合
- **前置**：读取最终章节内容（`chapters/`�?


