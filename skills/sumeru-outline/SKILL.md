---
name: sumeru-outline
description: 小说大纲设计：世界观构建、人物设定、剧情框架、分卷大纲、章节细纲
version: 1.2.1
type: skill
argument-hint: ["核心创意描述", "详细/精简/分卷", "复用数据", "允许映射"]
disable-model-invocation: false
user-invocable: true
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, task
context: project
agent: build
---

> 依赖 `sumeru-rules`，默认`quiet` 模式。
## 网文大纲设计

### 核心功能
1. **选题复用**：自动读取`plan.md`、`.sumeru/topic/options.json` 中的选题成果（旧路径兼容见`sumeru-rules/SKILL.md` 第六部分"独立调用自举协议"）2. **世界观设定*：世界背景、力量体系、社会规则、地理设定3. **人物设定**：主角、配角、反派的人物画像、性格、成长线，同步写入`characters/` 人物卡4. **剧情框架**：主线故事、支线剧情、关键节点、高潮安排5. **分卷大纲**：按卷划分剧情阶段，明确每卷核心冲突与目标6. **爽点排布**：规划关键爽点、转折点、悬念点的位置7. **伏笔管理表*：设计伏笔埋设、推进与回收计划
8. **强制合规检查*：识别可能的真实人名/地名，避免侵权风险
### 独立调用自举
1. 定位项目根目录，读取或生成`.sumeru/project.json`、`.sumeru/status.json`
2. 若已有`plan.md`、`.sumeru/topic/summary.json`，复用既有选题和需求（旧路径兼容见 `sumeru-rules/SKILL.md` 第六部分"独立调用自举协议"）3. 若缺乏`plan.md`，提示先执行 `sumeru-topic` 选题策划，或生成最小创意库存4. 若存在`.sumeru/creative-anchors.md`，读取创意锚点并注入大纲设计（锚点是不可违背的创意基准）
5. 大纲完成后生成或刷新 `outlines/chapters.json`；长篇项目同步拆分6. 同步生成 `characters/` 人物卡7. 若 `project.json.volumeCount ≥ 2`，执行"分卷模式支持"中的卷目录创建和跨卷初始化8. 更新 `.sumeru/status.json`、`.sumeru/cache/` 相关摘要

### 按模式输出| 模式 | 大纲输出 |
|------|----------|
| `short/light` | `outline.md`，包含高概念、人物、三幕结构、核心反转、情绪曲线|
| `medium/standard` | `plan.md`、`outline.md`、`outlines/chapters.json`、`characters/` |
| `long/full` | `plan.md`、`outline.md`、`outlines/chapters.json`、`characters/`、`world.md`，必要时拆分章节任务卡|

### 分卷模式支持

> 当 `.sumeru/project.json` 中 `volumeCount ≥ 2` 时启用分卷隔离模式，完整协议见 `sumeru-rules/SKILL.md` 第十五部分。

**卷目录创建**：大纲阶段在 `chapters.json` 生成前，检查 `project.json.volumeCount`：
- `volumeCount < 2` 或缺失 → 扁平模式，不变
- `volumeCount ≥ 2` → 执行以下操作：

1. **创建卷目录结构**：调用 `sumeru-rules` 第十五部分·四「分卷目录结构」创建 `.sumeru/volumes/vol-001/`、`vol-002/` ... `vol-{volumeCount}/`，每卷含 `continuity/`、`cache/`、`characters/`、`outline.md` 子目录
2. **创建跨卷目录**：创建 `.sumeru/cross-volume/` 含 `dependency-table.md`、`master-timeline.md`、`master-characters.md`
3. **注册卷信息到 project.json**：填充 `volumes` 映射，每卷标记 `status: "planned"`

**per-volume chapters.json 规则**：
- 主 `outlines/chapters.json` 仍保留所有章节的完整任务卡（全局统一索引）
- 每卷的 `.sumeru/volumes/vol-N/outline.md` 自动生成该卷的章节范围摘要（第 XXX-YYY 章）、核心冲突、本卷角色列表
- `outline.md` 的「分卷大纲」section 中每卷增加 `chaptersRange` 字段标注章节范围

### 项目化输出要求- `plan.md`：需求、世界观、人物、风格、创意策略、术语合并维护- `outline.md`：故事架构、主线、支线、分卷规划、关键高潮、伏笔管理表、节奏规划- `outlines/chapters.json`：章节任务卡主文件，每章必须包含全部必填字段（见下文"章节任务卡格式），旧路径兼容见 `sumeru-rules/SKILL.md` 第六部分"独立调用自举协议"
- `characters/`：人物卡目录，每人物独立文件
- `world.md`：世界观手册（long/full 模式）
> ⚠️ 生成 `outlines/chapters.json` 后必须执行章节任务卡输出验证质量门禁，缺字段不标记完成。
### 章节任务卡格式（`outlines/chapters.json`）
每章任务卡必须包含以下全部字段，供`sumeru-write` 子Agent直接使用）
```json
{
  "chapter": "001",
  "title": "章节标题",
  "purpose": "本章在全书中的核心作用,
  "events": ["事件1", "事件2", "事件3"],
  "openingHook": "本章具体开场方式（如：赵无缺从网吧出来，兜里没钱，正盘算去哪蹭饭）",
  "outputs": ["本章必须产出的内容],
  "acceptanceCriteria": ["验收条件1", "验收条件2"],
  "creativeGoal": "本章创意目标（如：让读者记住主角的第一次选择）,
  "freshnessHook": "本章新鲜感来源（如：反常规的师徒关系）,
  "emotionalBeat": "压抑 →困惑 →恍然 →暗爽",
  "emotionalBeatTemplate": "C",
  "readerMemoryPoint": "读者合上书还能记住的一幕（如：主角跪下那一刻）",
  "tropeToAvoid": ["本章要避开的套路（如：突然天降贵人）],
  "surpriseTwist": "本章反转（可选，若有则必须提前埋线索）,
  "protectedElements": ["底线元素（如：主角不能暴露真正实力）"],
  "allowedDeviations": ["允许的自由发挥（如：可以加人物小习惯）],
  "rhythm": "fast/medium/slow",
  "pacingNote": "节奏说明（如：前慢后快，结尾加速）"
}
```

**字段说明**）
| 字段 | 必填 | 说明 |
|------|------|------|
| `chapter` | ✅| 章节号（三位数） |
| `title` | ✅| 章节标题 |
| `purpose` | ✅| 本章在全书中的核心作用|
| `events` | ✅| 本章核心事件列表（≥3个，必须包含本章开场方式，禁止使用"场景建立""人物状态等泛化描述） |
| `openingHook` | ✅| 本章具体开场方式——一段可执行的场景描述而非概括（如"赵无缺从网吧出来，兜里还剩十八块钱），禁止"被X叫醒""从床上醒来"等模板式开场|
| `outputs` | ✅| 本章必须产出的内容|
| `acceptanceCriteria` | ✅| 验收条件（父Agent校验用） |
| `creativeGoal` | ✅| 本章创意目标 |
| `freshnessHook` | ✅| 本章新鲜感来源|
| `emotionalBeat` | ✅| 情绪走向（具体情绪词）|
| `emotionalBeatTemplate` | ✅| 情绪模板ID（A-G）|
| `readerMemoryPoint` | ✅| 读者记忆点 |
| `tropeToAvoid` | ✅| 要避开的套路列行|
| `surpriseTwist` | ❌| 反转设计（可选） |
| `protectedElements` | ✅| 底线元素（不可违背） |
| `allowedDeviations` | ❌| 允许的自由发挥（可选） |
| `rhythm` | ✅| 节奏：`fast`/`medium`/`slow` |
| `pacingNote` | ❌| 节奏补充说明（可选） |

### 章节任务卡输出验证（质量门禁）
`outlines/chapters.json` 生成后，父Agent必须执行以下验证，全部通过才允许标记 `outline` 阶段完成）
```markdown
## chapter.json 字段完整性检查
| 章节 | chapter | title | purpose | events | openingHook | outputs | acceptanceCriteria | creativeGoal | freshnessHook | emotionalBeat | emotionalBeatTemplate | readerMemoryPoint | tropeToAvoid | protectedElements | rhythm | 结果 |
|------|---------|-------|---------|--------|--------------|---------|-------------------|--------------|---------------|---------------|-----------------------|-------------------|---------------|-------------------|--------|------|
| 001  | ✅     | ✅   | ✅     | ✅    | ✅          | ✅     | ✅               | ✅          | ✅           | ✅           | ✅                   | ✅               | ✅           | ✅               | ✅    | ✅  |
| 002  | ❌     | ✅   | ❌     | ❌    | ❌          | ❌     | ❌               | ❌          | ❌           | ❌           | ❌                   | ❌               | ❌           | ❌               | ❌    | ❌  |
```

**验证规则**）- 每章必须包含全部 **15 个必填字段*：`chapter`、`title`、`purpose`、`events`、`openingHook`、`outputs`、`acceptanceCriteria`、`creativeGoal`、`freshnessHook`、`emotionalBeat`、`emotionalBeatTemplate`、`readerMemoryPoint`、`tropeToAvoid`、`protectedElements`、`rhythm`
- 任意一章缺字段 →**不通过**，父Agent不得标记 outline 完成
- 不通过时父Agent提示用户：`⚠️ chapters.json 第X章缺少字段 purpose, events, openingHook, ...，请重新运行大纲生成或手动补全`
- 通过后在 `.sumeru/changelog.md` 记录：`✅chapters.json 字段完整性验证通过（共N章）`

**字段质量规则（额外检查，不阻塞但标记警告）*）
| 规则 | 检查方式| 警告信息 |
|------|----------|----------|
| `events` 必须 ≥3 项| `len(events) >= 3` | `⚠️ 第X章events 少于3项，子Agent可能缺乏可执行事件` |
| `events` 不得包含泛化描述 | 关键词匹配（"建立""开端"引入""状态）| `⚠️ 第X章events 含泛化词"XXX"，请替换为具体可执行事件` |
| `openingHook` 不得为模板开场 | 关键词匹配（"被叫醒""从床上醒来""新的一天）| `⚠️ 第X章openingHook 使用了模板式开场"被X叫醒"，请指定独特的开场场景` |
| `openingHook` 必须为一句话场景 | 长度 > 10 字且包含具体场景元素 | `⚠️ 第X章openingHook 太简短，需要一段可执行的场景描述` |

**兼容模式**：若用户选择跳过此验证（通过 `--skip-validation` 参数），父Agent记录分`.sumeru/decisions.md` 并继续，但写作阶段不保证正文质量。
### 人物卡格式（`characters/*.md`）
每人物独立文件，格式如下）
```markdown
# {人物名}

## 基础信息
- **姓名**：（）
- **年龄**：（）
- **身份**：（）
- **外貌特征**：（3-5个关键特征，便于写作时保持一致）

## 性格画像
- **核心性格**：（3-5个关键词，如：冷峻、护短、嘴硬心软）
- **说话风格**：（如：简短、爱用反问、口头禅）
- **行为习惯**：（如：紧张时搓手指、思考时转笔）
- **内在矛盾**：（如：既渴望认可又害怕依赖）
- **差异化锚点**：（与同类角色的核心区别，如：同样是废柴，但他知道自己是废柴且接受）

## 人物弧光
- **起点状态**：（出场时的状态）
- **关键转折**：（2-3 个改变人物的节点）
- **终点状态**：（故事结束时的状态）

## 核心驱动力
- **核心欲望**：（最想要什么）
- **核心恐惧**：（最害怕什么）
- **底线**：（绝不会做的事）
## 人物关系
| 对象 | 关系 | 当前状态| 变化趋势 |
|------|------|----------|----------|
| 主角 | 师兄 | 信任 | →背叛 |

## 战力/能力
- **能力等级**）- **特殊技能*）- **限制/代价**）
## 人物标签（供检索）
`#反派` `#师门` `#第卷出场`
```

**人物卡质量要求*）- 核心性格必须有内在矛盾"（单一性格=纸片人）
- 人物弧光必须有明确转折（无弧入工具人）
- 每个人物必须有独立欲望（无欲有被剧情推着走）

### 创意架构要求
- **高概念锁定*：选择为pitch，写入`plan.md`
- **类型混血控制**：说明主类型承诺和嫁接类型的边界
- **反套路策略*：列出本书最容易俗套的5 个桥段，并给出替代写法- **卷级惊喜**：每卷至少设设1-3 为意外但合理的反转- **情绪曲线**：规划每卷主导情绪和相邻章节情绪差异
- **角色主动性压力测试**：主角、反派、重要配角都必须有独立欲望和主动选择
- **伏笔管理表*：大纲阶段必须输出伏笔清单，写入 `outline.md`。每项伏笔包含：
  - `id` —唯一编号（`F1`、`F2` ...）  - `内容` —伏笔的具体描述  - `类型` —设定伏笔 / 人物伏笔 / 道具伏笔 / 事件伏笔
  - `埋设位置` —第几卷·第几章·场景
  - `预期回收位置` —第几卷·第几章
  - `状态` —已埋 / 待收 / 已收
  - `备注` —可选，说明回收条件或替代方案- **情绪节拍模板池与轮换**（新增）：每章`emotionalBeat` 必须从模板池中按轮换规则选取，避免连续多章使用同一情绪走向

### 情绪节拍模板要求
> **规则**：连续3 章不得使用同一模板。卷首章优先用B（递进型）或D（双线型），卷尾章优先用 A（反转型）或 C（抑扬型）。
| 模板ID | 名称 | 情绪走向 | 适用场景 |
|--------|------|----------|----------|
| A | 反转型| 期待 →落空 →意外 →爆| 反转章节、打脸、揭秘|
| B | 递进型| 平静 →波动 →升级 →爆发 | 战斗、冲突升级、成长突破|
| C | 抑扬型| 低谷 →挣扎 →转机 →希望 | 战败后重整、低谷反弹、日常后的危机|
| D | 双线型| A线紧张 + B线松弛 →交汇 →引爆 | 多线叙事、伏笔推进、平行场景|
| E | 悬念型| 异常 →试探 →揭示 →新的未知 | 悬疑推进、世界观揭示、阴谋铺展|
| F | 情绪型| 抵触 →触动 →接纳 →升温 | 人物关系变化、情感线推进 |
| G | 低谷型| 压制 →崩溃边缘 →觉醒 →反击 | 大挫折、绝境重生、战力觉醒|

**分配策略**）- 写作前由父Agent扫描前章已用模板，排除重复选项
- 从剩余模板中按场景最适合优先"原则选择，不强制随机
- 模板ID写入 `outlines/chapters.json` 每章任务卡的 `emotionalBeatTemplate` 字段
- `emotionalBeat` 的具体情绪词根据模板框架和本章情节细化
### 节奏规划要求

大纲必须为每章规划叙事节奏，写入任务卡的 `rhythm` 字段）
| 节奏 | 特征 | 适用场景 | 每卷占比建议 |
|------|------|----------|-------------|
| `fast` | 事件密集、对话紧凑、少描写、多冲突 | 高潮、战斗、反转、追逐| 30-40% |
| `medium` | 事件推进+适度铺垫、有对话有描写| 日常推进、信息获取、关系发展| 40-50% |
| `slow` | 环境渲染、内心独白、生活细节、伏笔铺型| 开篇、低谷、转折前蓄力、情感铺型| 10-20% |

**节奏规则**）- 连续 2 章`fast` 后必须插入`medium` 或`slow`（读者需要喘息）
- 连续 3 章`slow` 是上限（超过读者流失）
- 卷首章建设`medium` 或`slow`（建立场景）
- 卷尾章建设`fast`（收束有力）
- 每卷至少 1 为`fast` 高潮点
**节奏规划行*（写入`outline.md`）：

```markdown
## 节奏规划

| 章节 | 节奏 | 说明 |
|------|------|------|
| 001-003 | medium →fast | 开篇引入，第章第一个高潮|
| 004-006 | slow →medium | 低谷蓄力，铺垫师门背景|
| 007-010 | fast | 连续冲突，卷末爆取|
```

### 支线规划要求

大纲必须明确规划支线剧情，写入`outline.md`）
**支线定义**）- 支线必须有独立的"启动 →发展 →收束"结构
- 支线必须与主线有交汇点（推动主线或受主线影响）- 支线不能喧宾夺主（占比≥主线的30%）
**支线规划格式**（写入`outline.md`）：

```markdown
## 支线规划

### 支线1：{支线名称}
- **类型**：人物线 / 世界观线 / 情感线/ 势力线- **启动章节**：第X章- **收束章节**：第Y章- **核心冲突**：一句话描述
- **与主线交求*：在第Z章推动主线XXX
- **关键节点**）  - 第X章：启动事件
  - 第X+3章：发展/升级
  - 第Y章：收束/解决

### 支线2：{支线名称}
...
```

**支线数量建议**）- 短篇（≤50章））-2 条支线- 中篇）0-150章））-4 条支线- 长篇（≥150章））-6 条支线
**支线禁忌**）- ❌支线启动后无收束（烂尾线）- ❌支线与主线完全无关（注水线）
- ❌支线角色比主线角色更吸引人（喧宾夺主）
### 大纲自检项（质量门禁）
大纲完成后必须逐项检查（共计 32 项，分 6 类），全部通过才允许进入写作阶段：

#### 主线完整性检查
| # | 检查项 | 不通过处理 |
|---|--------|-----------|
| 1 | 主线是否有明确的"起点 →冲突 →高潮 →结局"）| 补充缺失环节 |
| 2 | 主角是否有清晰的成长弧光）| 设计 2-3 个转折点 |
| 3 | 核心冲突是否有足够的驱动力？ | 强化冲突 stakes |
| 4 | 结局是否回应了开篇的核心问题）| 确保闭合 |

#### 伏笔完整性检查
| # | 检查项 | 不通过处理 |
|---|--------|-----------|
| 5 | 每个伏笔是否都有预期回收位置）| 补充回收计划 |
| 6 | 是否存在"悬空伏笔"（无回收计划）？ | 决定删除或补充回收|
| 7 | 伏笔回收是否在合理的时间窗口内？ | 过早回收=浪费，过景遗忘 |
| 8 | 高频伏笔（≥3次提及）是否有升线变体）| 避免重复提及无进展|

#### 人物质量检查
| # | 检查项 | 不通过处理 |
|---|--------|-----------|
| 9 | 主角是否有独立欲望（不是被推着走））| 设计主动选择节点 |
| 10 | 反派是否有合理动机（不是纯坏）？ | 补充反派 backstory |
| 11 | 重要配角是否有独立弧光？ | 至少设计 1 个转技|
| 12 | 人物关系是否有变化（不是静态））| 设计关系转折点|

#### 节奏与爽点检查
| # | 检查项 | 不通过处理 |
|---|--------|-----------|
| 13 | 每卷是否有至就1 为读者能记住的名场面"）| 设计名场面种存|
| 14 | 爽点分布是否均匀（不能集中在某一卷））| 重新排布 |
| 15 | 低谷后是否有明确的反弹设计？ | 补充反弹节点 |
| 16 | 节奏是否有松有紧（不能全程高能或全程平淡）？ | 调整节奏规划 |

#### 创意质量检查
| # | 检查项 | 不通过处理 |
|---|--------|-----------|
| 17 | 是否有只属于本书的独特设定？ | 强化差异化 |
| 18 | 反套路策略是否已落实到具体章节？ | 检查tropeToAvoid |
| 19 | 每卷是否有意外但合理的反转？ | 设计卷级反转 |
| 20 | 金手指是否有代价/限制（不能无敌）| 补充限制条件 |
| 21 | **套路查重**：主角设定是否与近年热门作品高度雷同？ | 重新设计差异化锚点 |
| 22 | **套路查重**：核心反转是否是"天降贵人/突然觉醒/隐藏实力"三件套？ | 换反转逻辑 |
| 23 | **套路查重**：伏笔是否只有身世之谜/神秘残片/黑衣人？ | 补充非常规伏笔类型 |
| 24 | **锚点检查**：创意锚点是否已体现在大纲中？ | 补充锚点落地 |

#### 跨卷连续性检查
| # | 检查项 | 不通过处理 |
|---|--------|-----------|
| 25 | **伏笔跨卷追踪**：伏笔管理表中的每项伏笔是否有明确的所属卷和预期回收卷？是否存在跨卷伏笔被"遗忘"（即伏笔埋在第 N 卷，但之后再无提及）？ | 标注跨卷伏笔 → 在后续卷中安排至少 1 次提及节点 |
| 26 | **人物成长跨卷一致性**：主角/重要配角的实力、心性、关系在第 N 卷末尾的状态，是否与第 N+1 卷开头衔接？是否存在上一卷重伤下一卷无伤、上一卷决裂下一卷和好等无因跳跃？ | 在第 N+1 卷首章安排 1 个状态承接场景，注释须说明承接关系 |
| 27 | **力量体系跨卷稳定**：新卷引入的新能力/新等级是否在上卷逻辑可推演范围内？是否存在回忆杀式解锁"上卷从不提现本卷突然觉醒"的断层？ | 在上卷对应位置补充暗示线索，或在本卷安排合理的觉醒契机 |
| 28 | **时间线跨卷连续**：各卷之间的时间间隔是否合理？是否出现"上卷还在秋天本卷已经五年后"而无任何过渡交代？ | 在卷首明确标注时间流逝，或设计时间跳跃的叙事锚点 |
| 29 | **人物关系跨卷演变**：人物关系（敌友/信任/冲突）在各卷间的变化是否有因果链条？前卷建立的关系张力是否在后续卷有推进或收束？ | 标注每对关系的"卷际状态卡"→ Ensure 每卷至少推进 1 条关系线 |
| 30 | **卷间情绪曲线平滑度**：上一卷的卷尾情绪（悲/喜/悬/惊）与本卷的卷首情绪是否衔接自然？是否出现上卷结尾大悲本卷开头嬉笑打闹的情绪断层？ | 在卷首章设计 200-500 字情绪过渡段 |
| 31 | **跨卷设定一致性**：前卷建立的设定规则（如功法消耗、道具限制、势力边界）在本卷是否有违背？本卷新引入的设定是否与前卷设定矛盾？ | 新设定需改写为上卷的"伏笔回收"或"设定扩展"，而非凭空新增 |
| 32 | **卷间节奏过渡**：上一卷尾章的节奏（fast/medium/slow）与本卷首章的节奏是否合理衔接？是否出现"上卷末尾激战高潮 → 本卷开头日常慢热"的节奏断崖？ | 如果节奏变化过大，在卷首章前段安排节奏过渡段（如激战后处理→从战场撤退 → 过渡到新场景） |

**跨卷检查补充工具**：大纲阶段完成后，建议在 `outline.md` 末尾追加一张**跨卷依赖表**：

```markdown
## 跨卷依赖表

| 依赖类型 | 内容 | 源卷 | 目标卷 | 依赖说明 |
|----------|------|------|--------|----------|
| 伏笔 | F3 黑衣人身份 | 卷1·005 | 卷3·012 | 卷1埋设，卷3回收 |
| 成长 | 主角练气三层→筑基 | 卷1→卷2 | 卷2尾 | 卷1末尾练气三层→卷2经历3次战斗后突破 |
| 关系 | 主角↔苏瑾 信任→怀疑 | 卷2·008 | 卷3·010 | 卷2结盟 → 卷3因误会生嫌 |
| 设定 | 黑色残片吸收上限 | 卷1·intro | 卷3 | 卷1限定吸收3次 → 卷3突破上限需新设定补充 |
```

此表写入 `outline.md`，供 `sumeru-review` 在审查阶段做跨卷校验。**

**自检输出格式**）
```markdown
## 大纲自检报告

| 检查项 | 状态| 说明 |
|--------|------|------|
| 1. 主线闭合 | ✅| 起点→宗门考核→最终决战→回归 |
| 2. 主角弧光 | ✅| 3个转折点：觉醒、背叛、顿悟|
| 5. 伏笔回收 | ⚠️ | F3（黑衣人身份）未规划回收位置 |
| 9. 主角主动性 | ✅| 每卷至少2个主动选择 |
| 25. 伏笔跨卷追踪 | ⚠️ | F1（残片来历）在卷1·003埋设后，卷2全卷无提及 |
| 26. 人物成长跨卷 | ✅ | 无无因跳跃 |
| 30. 卷间情绪曲线 | ⚠️ | 卷2尾大悲→卷3首嬉笑，缺200-500字情绪过渡段 |
| ... | ... | ... |

**需修复项**）
- F3 伏笔建议在第3卷第15章回收
- 卷2→卷3过渡需在卷3首章加情绪过渡段
```

### 合规约束规则
1. **人名约束**：禁止使用真实人名（历史人物、公众人物、知名IP角色名）
2. **地名约束**：禁止使用真实地名（国家名、城市名、山脉名等）
3. **自动检查*：发现疑似真实名称时自动提示并提供个以上虚构替换方案
### 子Agent并行细纲生成

> **并行规则**详见 `sumeru-rules/SKILL.md` 第二部分"子Agent并行处理规则"。
> **卷→章拆分参考**详见 `references/volume-chapter-split.md`，子Agent拆分前必须参考其中的决策树、卷类型模板和约束合规检查表。
当章节数大于3章时，支持子Agent并行生成细纲。每个子Agent最多负责章，按卷分配优先。
**细纲输出格式（写入`outlines/chapter-XXX-YYY.md`）*）
每章必须包含以下信息，后续供 `sumeru-write` 子Agent的context pack 使用）
```markdown
## 第X章：章节标题

| 项目 | 内容 |
|------|------|
| **情绪模板** | X |
| **章节目的** | 一句话说清本章在全书中的作用|
| **圣杯状态* | 当前圣杯状态|
| **开场方式* | 本章第一句话/第一个场景具体描写（禁止"被X叫醒"、禁止新的一大）|

### 剧情推进
1. 具体可执行的事件1
2. 具体可执行的事件2
3. 具体可执行的事件3
4. ...（≥3个）
```

**细纲质量规则**）- `events`（剧情推进）必须 ≥3 项，且每项是"主语+动词+对象"的具体可执行事件，不是场景建立""情感过渡"等泛化描述- `开场方式` 必须指定一个独特的场景开端，禁止连续两章以相同方式开场（如同一批的 3 章都用人物醒来"开头）

### 数据持久化**用户可见输出**）- `plan.md`、`outline.md`、`outlines/chapters.json`、`characters/`、`world.md`

**中间数据（`.sumeru/outline/`）*）- 仅保存必要缓存；旧版 `world.json`、`characters.json`、`plot-outline.json`、`chapter-outlines.json` 只读兼容（旧路径兼容策略见`sumeru-rules/SKILL.md` 第六部分"独立调用自举协议"）

**分卷模式**（`volumeCount ≥ 2` 时额外创建）：
- `.sumeru/volumes/vol-N/continuity/`、`cache/`、`characters/`、`outline.md`
- `.sumeru/cross-volume/dependency-table.md`、`master-timeline.md`、`master-characters.md`
### 风格样本自动分析

> **触发**：用户在大纲完成后提供 `style-samples/user-sample-<timestamp>.md` 时（模板见 `style-samples/sample-template.md`）。
> **机制定义**：`sumeru-rules/SKILL.md` 第十二部分"风格样本说明"

**自动分析流程**：

1. 检测 `style-samples/user-sample-*.md` 是否存在
2. 若存在，父 Agent 读取样本，按 6 维度分析：
   - **用词习惯**（口语化程度/口头禅/书面口语比例）
   - **句式特点**（平均句长/断句频率/主谓残缺/重复标点）
   - **对话风格**（完整性/潜台词/口头禅/自我纠正）
   - **情绪表达**（直接 vs 动作承载/情绪矛盾/身体反应）
   - **比喻/意象**（个人化程度/精准度/频率/类型偏好）
   - **叙事节奏**（冗余程度/逻辑跳跃/留白频率/过渡方式）
3. 把分析结果写入 `style-samples/user-style.md` 的对应表格（替换"待分析"占位）
4. 写入后，user-style.md 自动作为 sumeru-write 后续每章 context pack 的固定组成部分

**注意**：
- user-style.md 仅首次生成后保留手动修改，后续不覆盖
- 用户未提供样本时，user-style.md 保持"待分析"状态，后续写作以 `plan.md.creativeStrategy` 兜底
- "禁止项"段：若用户提交样本中明确说明"不要写 XX"，必须在 user-style.md 的禁止项段原样保留

_（小说简介生成已移至 `sumeru-worldbuilder`）_

### 与其他Skill 配合
- **前置**：`sumeru-topic`（选题策划），可复用已有`plan.md`
- **后续**：`.sumeru/intro.md` 可供 `sumeru-finalize` 在发布导出时复用简介；`sumeru-write` 可参考其中的传播句作为章节结尾钩子；`characters/` 和`world.md` 供全部下游技能使用
