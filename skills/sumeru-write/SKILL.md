---
name: sumeru-write
description: 小说章节内容创作与创意落地。用户要写一章小说、续写、扩写、重写、生成某个情节、按细纲写章节、批量生成章节、写开篇/高潮/过渡章，或要求"帮我写小说内容"、风格样本注入时必须使用本技能。
type: skill
---

> ⚠️ **依赖技能**：本 Skill 依赖 `sumeru-rules` 中的全局约束。执行前请确保已加载。

> 📌 **输出级别**：默认 `quiet` 模式，只输出进度。详细规范见 `sumeru-rules` "输出级别规范"。

> 📌 **写作模式**：使用通用写作模式。

## 网文章节撰写

### 触发关键词
帮我写一章小说、续写接下来的内容、生成XX情节、批量写网文章节、扩写/重写这段内容、帮我写个XX情节、续写小说、把这段内容扩写、重写这一章、批量生成小说章节、写个开篇章节、写个高潮情节、小说内容生成、帮我写小说内容、网文章节生成、从细纲生成章节、按细纲写小说、批量生成所有章节、细纲驱动写作

### 核心功能
1. **基于完整细纲生成**：自动读取 `outlines/chapters.json`（兼容 `.sumeru/outline/chapter-outlines.json`）
2. **智能细纲匹配**：支持按章节号、卷号、或全部章节进行生成
3. 自动适配网文节奏：开头抓眼球、中间有冲突、结尾留悬念
4. 保持人物性格、剧情逻辑的一致性
5. 支持自定义章节长度（默认4000-5000字/章）
6. 支持续写、修改、调整已有章节内容

### 独立调用自举
1. 定位项目根目录，读取或生成 `.sumeru/project.json`、`.sumeru/status.json`
2. 若缺少 `outlines/chapters.json`，尝试读取 `.sumeru/outline/chapter-outlines.json`（兼容旧路径）
3. 若缺少 `.sumeru/cache/`，生成最小摘要
4. 若缺少当前范围的 context pack，先生成临时 context pack 再写作
5. 根据 `chapters/` 已有文件推断续写位置，默认不覆盖已有章节
6. **写正文必须走子agent（即使是单章续写），父agent绝不写正文**

### 通用写作模式

**核心原则：所有章节统一使用同一套写作规则。**

章节差异只通过任务卡、用户要求、目标字数、情绪节拍和剧情功能控制。

通用写作模式包含：
- 任务卡执行规则
- 剧情统一门禁
- 创意落地五问
- 人物真实感与毛边规则
- 写后状态标记与一致性校验

### 输入优先级
1. 用户本次明确要求（章节号、字数、风格、视角、必须出现/禁止出现的情节）
2. `.sumeru/context-packs/write-<range>.md`（批量写作首选）
3. `.sumeru/cache/` 摘要（project-brief、style-brief、creative-brief、continuity-brief）
4. `.sumeru/review/fix-plan.json` 中标记的重写要求
5. `outlines/chapters.json` 中的目标章节任务卡（兼容旧路径 `.sumeru/outline/chapter-outlines.json`）
6. 已存在的 `chapters/` 内容用于续写和风格衔接

### 剧情统一门禁

写作前父Agent必须把以下内容写入 context pack，并要求子Agent严格遵守：

1. **剧情事实基准**：上一章实际结尾、当前时间线、当前地点、主线目标、卷级冲突。
2. **人物状态基准**：相关人物的位置、伤势、战力、关系、情绪状态、说话风格。
3. **物品与能力基准**：关键道具归属、是否损坏/消耗、能力限制、冷却或代价。
4. **伏笔状态基准**：已设置、推进中、已回收、禁止重复激活的伏笔。
5. **禁止改写事实**：不得改变已发生事件、已确定人设、已回收伏笔、既定战力体系和章节结尾钩子。

写作后父Agent必须执行剧情统一校验：

1. 解析 `SUMERU_STATUS` 中的 `state_diff`、`char_update`、`plot_update`。
2. 与 `.sumeru/continuity/consistency-rules.json`、最近 3 批摘要、上一章实际结尾对比。
3. 检查人物位置冲突、道具状态冲突、时间线倒置、战力无因跳跃、伤势无因恢复、已回收伏笔重复激活。
4. 发现 `critical` 或 `high` 冲突时，暂停写入正式章节，只生成 issue 或 fix-plan。
5. 只有剧情统一校验通过后，才允许更新 `chapters/`、`.sumeru/status.json` 和 continuity cache。

### 章节任务卡执行规则

**核心规则：**
- 正文必须完成任务卡中的 `purpose`
- 正文必须落实 `creativeGoal`、`freshnessHook`、`emotionalBeat`、`readerMemoryPoint`
- 遇到 `tropeToAvoid` 时必须避开直给套路
- 若存在 `surpriseTwist`，必须提前埋下公平线索
- 正文必须满足全部 `acceptanceCriteria`

**创意自由度控制：**
```json
{
  "creativeFreedom": "medium",
  "protectedElements": ["主角必须展现判断力", "不能暴露真正实力"],
  "allowedDeviations": ["可以添加人物小习惯", "可以有对话停顿/口误"]
}
```

**优先级规则：**
```
protectedElements（底线） → 不可违背
    ↓
purpose（必须完成） → 可以改变"如何完成"的方式
    ↓
allowedDeviations（鼓励添加） → 不强制，但鼓励
```

**protectedElements 执行保障：**
- 子Agent严格按 protectedElements 写作，不做任何例外申请
- 子Agent输出后，父Agent对比 state_diff 与 protectedElements，检测是否违反
- 发现违反时，父Agent暂停并询问用户确认

### 创意落地检查（生成章节前自检）
1. 本章是否有读者能记住的一幕、一个选择或一句话
2. 本章爽点是否和前 3 章重复
3. 主角是否通过选择推动局势，而不是被剧情推着走
4. 反派或阻力是否足够聪明，是否让胜利付出代价
5. 结尾钩子是否既承接本章输出，又诱导下一章点击

### 细纲驱动批量生成
```
/sumeru-write 全部章节       # 生成所有章节（自动并行）
/sumeru-write 第1-50章       # 生成指定范围
/sumeru-write 第1卷          # 生成特定卷
/sumeru-write 第3章 "概要"   # 单章创作
/sumeru-write 第3章 按细纲生成
```

### 章节文件命名规范
**强制格式：`{三位章节号}-{章节标题}.md`**
- ✅ `003-第一次解析.md`、`001-废物觉醒系统.md`
- ❌ `第3章-第一次解析.md`、`3-第一次解析.md`、`003_第一次解析.md`

### 正文标题格式规范
**强制格式：正文第一行必须为 `第X章 标题xxxx`**
- ✅ `第1章 离婚协议落在雨里`
- ✅ `第2章 雨夜告别`
- ❌ `第一章 离婚协议落在雨里`（不能用中文数字）
- ❌ `1. 离婚协议落在雨里`（不能用阿拉伯数字+点）
- ❌ `001-离婚协议落在雨里`（不能用短横线）

**格式规则：**
- 章节号：阿拉伯数字，不加前导零（`第1章` 不是 `第001章`）
- 标题：与文件名中的章节标题一致
- 第一行：必须是标题行，后面跟空行，然后是正文

### 数据持久化
**用户可见输出**：`chapters/` 下的纯净正文文件

**中间数据（`.sumeru/write/`）**：
- `progress.json`、`chapter-meta.json`、`character-state.json`、`original/`

### 与其他 Skill 配合
- **前置**：`sumeru-outline` 生成的 `plan.md`、`outline.md`、`outlines/chapters.json`
- **后续**：供 `sumeru-review`、`sumeru-polish`、`sumeru-finalize` 使用

---

### 风格样本强制注入

**写作前必须读取 `style-samples/user-style.md`，并嵌入 context pack。**

#### 前置检查

启动 `sumeru-write` 前，父 Agent 必须检查：
1. `style-samples/user-style.md` 是否存在
2. 如果不存在，提示用户先完成风格样本采集（见 `sumeru-outline` 的"风格样本采集"步骤）
3. 只有样本存在，才允许启动写作

#### Context Pack 中新增

```markdown
## 用户风格特征（来自样本）

（从 `style-samples/user-style.md` 读取）

### 模仿要求
- 用词习惯：模仿样本中的口语化表达
- 句式特点：模仿样本中的短句、断裂句
- 对话风格：模仿样本中的停顿、改口、口头禅
- 情绪表达：模仿样本中的直接/间接比例
- 比喻风格：模仿样本中的个人化程度

### 禁止项
- 不要写得"太正确"
- 不要过度优化
- 不要删除冗余
```

#### 子 Agent 学习要求

子 Agent 必须：
1. 阅读 `style-samples/user-style.md`，分析用户特征
2. 写作时主动模仿样本中的特征
3. 输出后自检：有没有"太正确"的地方？（如果有，对照样本修改）

#### 风格对比自检

```markdown
## 风格对比自检

### 对照样本检查
- [ ] 用词习惯是否接近样本？（口语化 vs 书面化）
- [ ] 句式特点是否接近样本？（短句/断裂 vs 完整/规范）
- [ ] 对话风格是否接近样本？（停顿/改口 vs 完整清晰）
- [ ] 情绪表达是否接近样本？（直接/间接比例）
- [ ] 比喻风格是否接近样本？（个人化 vs 通用化）

### 如果差异太大
- 回到样本，重新分析特征
- 修改"太正确"的部分
- 保留"奇怪"的部分
```

### 全局约束引用
> 完整全局约束见 `sumeru-rules` 技能，核心要点如下：
> - **子Agent规则**：写正文必须走子agent，最多5个并行，每个最多3章
> - **职责边界**：子Agent只读 context pack，输出纯结果+状态标记，不碰状态文件
> - **状态标记**：输出首行必须包含 `<!-- SUMERU_STATUS: chapter=X, status=Y, state_diff={JSON}, char_update={JSON}, batch=N, timestamp=... -->`
> - **Context Pack**：控制在1500-3000中文字，包含项目brief、人物摘要、世界观摘要、任务卡、批次摘要

---

### 毛边核心原则

**记住一句话：**

> **AI 写得越"正确"的部分越要改，写得越"奇怪"的部分越要保留。**

**毛边规则：**
- 禁止规则化毛边。不要求"每章几个毛边"，让子 Agent 自由决定。
- 可以完全没有毛边，也可以突然插入一个与主线无关的细节。
- 毛边不必须服务于人物、情绪或伏笔——有时候毛边就是毛边，没有功能。
- 如果某个细节让你觉得"这有点奇怪/不合逻辑"，先保留，不要删。

**如果不确定要不要加毛边：**
- 加 → 可能奇怪，但奇怪是真实的
- 不加 → 可能正确，但正确是可疑的

**宁可奇怪，不要正确。**

### 写作质感底线

- 对话可以句句完整，也可以突然跑题、重复、自我纠正。允许"没说完的话"，但不必须。
- 情绪可以直接说"我很生气"，也可以用身体反应承载，或者两者都有。允许情绪矛盾。
- 爽点可以有铺垫，也可以突然爆发。允许"反派嘲讽→主角直接反击"的直线套路，如果这样更真实。
- 每章可以有无意义的细节，不需要每个细节都服务人物、情绪或伏笔。
- 结尾可以有钩子，也可以突然结束、留白、没说完。

**核心原则：AI 写得越"正确"的部分越要改，写得越"奇怪"的部分越要保留。**

---

### 反 AI 写作规则

**核心原则：AI 写得越"正确"的部分越要改，写得越"奇怪"的部分越要保留。**

| 维度 | AI"正确"写法 | 人类"奇怪"写法 | 执行要求 |
|------|-------------|---------------|---------|
| **逻辑** | 因果完整、层层递进 | 突然跳跃、留白、不解释 | 每章 1-2 处逻辑跳跃，跳跃后需有"隐约可感知的关联"（同一意象/情绪逻辑） |
| **表达** | 精准、书面化、规范 | 模糊、口语化、不完美 | 允许笔误、模糊表达，但不破坏基础可读性 |
| **冗余** | 删除重复、精炼用词 | 重复、啰嗦、自我纠正 | 每章至少 1 处冗余 |
| **情绪** | 线性递进、层次分明 | 矛盾、混乱、说不清 | 情绪可以自我否定 |
| **对话** | 有目的、有潜台词 | 跑题、重复、没说出口的话 | 对话可以"没说完" |
| **细节** | 服务于情节 | 无意义、个人化 | 每章至少 1 个无意义细节 |
| **结尾** | 有总结、有钩子 | 突然、留白、没说完 | 允许没有总结句 |
| **语感** | 主谓宾齐全、标点规范 | 破碎句、倒装、重复标点、口头禅 | 允许"然后……然后……"、"？！……"、主谓残缺 |

**禁止项：**
- ❌ 禁止"情绪→动作→环境"三重对应（每次都凑齐）
- ❌ 禁止每章固定数量的"毛边"
- ❌ 禁止自检清单（"我这章够不够人性化"）
- ❌ 禁止"潜台词""节奏控制"等过度优化
- ❌ 禁止删除冗余（冗余是人工特征）
- ❌ 禁止"自然融入"（毛边可以不自然）
- ❌ 禁止"过度优化的具象比喻"（如强行用比喻解释抽象概念）

**奇怪的保留条件：**
- ✅ 奇怪但可理解 → 保留
- ⚠️ 奇怪到无法理解（主语缺失、时间顺序完全错乱、人名混淆）→ 微调，但不消除"奇怪感"

### 人味注入示例

| AI"正确"写法 | 人类"奇怪"写法（保留） |
|-------------|---------------------|
| 他紧张得手心出汗，心脏狂跳。 | 他手心出汗，但怪的是，心跳慢得像散步。 |
| "我不同意。"他直接反驳。 | "我不……其实也不是不同意，就是……"他挠挠头，没说完。 |
| 她赢了比赛，感到既兴奋又疲惫。 | 赢了。然后？然后她蹲下系鞋带，系了三次。 |
| 窗外下着雨，空气潮湿。 | 窗外的雨下得像个记性不好的人，停一下，想起来又下。 |
| 他感到非常愤怒。 | 他非常愤怒。愤怒到想砸东西，但又懒得动。 |
| "你走吧。"她说。 | "你走吧。"她说。说完又补了一句，"其实……算了。" |
| 雨下得很大。 | 雨下得挺大。砸在窗上噼里啪啦，像有人在敲。 |

---

### 自检清单（写作完成后检查）

```markdown
## 反 AI 自检

### 检查"正确"的地方
- [ ] 有没有逻辑太完整、因果太清晰的地方？（如果有，考虑打断）
- [ ] 有没有表达太精准、太书面化的地方？（如果有，考虑模糊化）
- [ ] 有没有冗余被过度删除？（如果有，考虑加回）
- [ ] 有没有情绪太线性、太"层次分明"？（如果有，考虑矛盾化）
- [ ] 有没有对话太有目的、太"有潜台词"？（如果有，考虑跑题）
- [ ] 有没有每个细节都有功能？（如果有，考虑加无意义细节）
- [ ] 有没有结尾有总结、有钩子？（如果有，考虑留白）

### 检查"奇怪"的地方
- [ ] 有没有逻辑跳跃？（保留，但需有隐约可感知的关联）
- [ ] 有没有模糊/不确定表达？（保留）
- [ ] 有没有冗余/啰嗦？（保留）
- [ ] 有没有情绪矛盾？（保留）
- [ ] 有没有对话跑题/重复？（保留）
- [ ] 有没有无意义细节？（保留）
- [ ] 有没有奇怪到无法理解？（如果是，微调但不消除"奇怪感"）

### 语感检查
- [ ] 有没有破碎句、倒装、重复标点？（允许有）
- [ ] 有没有口头禅（"然后……"、"就是说"、"那个"）？（允许有）
- [ ] 有没有主谓残缺？（允许有）

### 剧情统一（保留）
- [ ] 本章是否承接上一章实际结尾
- [ ] 人物位置、伤势、战力、关系是否与 continuity 一致
- [ ] 道具归属、消耗、损坏状态是否一致
- [ ] 伏笔是否只推进，不无因重置
- [ ] `state_diff` 是否完整记录本章变化
```
