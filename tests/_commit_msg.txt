fix: 补修上一轮漏检的 12 处损坏；移除死参数；扩展检测判据

## 背景

上一提交（5cbfcf7）声称「中文损坏清零」，但该结论**不成立**：
它依据的是检测器不再报警，而检测判据在排查过程中被逐步收窄，
反而掩盖了真实残留。实际产物中仍有 12 处损坏文本。

本轮改为**直接查字符串是否存在**来验证，不再以检测器输出为唯一依据。

## 补修的损坏（12 处）

- `wq-rules/SKILL.md`
  - L151/L152 批次摘要示例整块重写：`第-6章-` → `第1-6章`、
    `系统001)` → `系统(001)`、`首次出在` → `首次出场`、
    `消者构` → `消耗`、`黑衣人身从` → `黑衣人身份`、
    `伏笔线推过` → `伏笔线推进`，并拆回被压平的多行列表
  - L201/L202 `~1500-2000 存` / `~300-500 存` → 「字」
  - L347 `伏笔线推过` → `伏笔线推进`
  - L377 `更新人物状态4. 就\`status\`` → 拆行 + `就`→`依据`
  - L421 `**迁移规则**）1.` → `**迁移规则**：` + 拆行
  - L607 父Agent校验流程：6 个列表项被压成一行且缺第 3 步 → 拆行并补全
- `wq-outline/SKILL.md`
  - L29-30 编号项 2-8 粘连成两行 → 拆为 7 行
  - L168 `**能力等级**）- **特殊技能*）- **限制/代价**）` → 拆行
  - L170 `#第卷出场` → `#第N卷出场`
  - L231 `第章第一个高潮` → `第1章第一个高潮`
  - L248-250 支线字段 6 项粘连 + `交求` → `交汇` → 拆行
  - L378-379 `**圣杯状态*` / `**开场方式*` 加粗未闭合 + `新的一大` → `新的一天`

## 其他修正

- `wq-review/scripts/continuity-check.py`：移除 `--chapters` 参数。
  上一轮新增该参数仅为「兼容」，但代码从未引用它（死参数，误导使用者）。
  一致性校验本就是全量的，不支持按章过滤。
- `wq-revise/SKILL.md`：相应改回不带 `--chapters` 的调用，并注明该脚本
  不支持按章过滤。
- `tests/find-corruptions.py`：把本轮漏检的 12 个实例补入词典判据，
  防止同类残留再次逃过检测。

## 验证（改为直接断言，不依赖检测器）

- 12 个漏检字符串在 `skills/**/*.md` 中逐一断言**不存在**：残留 = 0
- 冒烟 19/19；损坏 0；围栏全平衡；加粗/粘连 0；体检 0 P0 / 0 P1；
  契约 0 冲突；文档命令失败 0/15；JSON 7/7 合法
